# Required — your Resend API key (get it from https://resend.com/api-keys)
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxx

# Optional — the "from" address for outgoing emails (must be a verified domain in Resend)
# Defaults to noreply@uncompromised.ai if not set
FROM_EMAIL=noreply@uncompromised.ai
